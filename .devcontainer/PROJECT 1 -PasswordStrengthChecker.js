// PROJECT 1 — Password Strength Checker (VERY SMALL)
// Why this first: strings, conditions, functions, confidence boost.
// Features
// Input: password (string)
// Output:
// Weak / Medium / Strong
// Rules:
// length < 6 → Weak
// contains number → +1
// contains uppercase → +1
// contains special char → +1
// Concepts Used
// strings
// if–else
// functions
// loops
let password;

 function PasswordChecker( ...password) {
    console.log("Enter a password :");
    //  let str;
    // if (str = password.length < 6) {
    //     console.log(`Password is Weak! Please enter a strong password `);
    // }
    // else {
    //     console.log(`Your Password $(password)`);
    // }

    // for(let i = 0 ;i < 10 ; i++){
    //     let str;
    //     if( string.includes(i)){
    //      return string;

    //     }
    //     else{
    //         console.log("At least enter a digit!");
    //     }
    // }

}
console.log(PasswordChecker('priyanka'));







